{
  "name": "console-get-logs",
  "description": "Recent Unity Console entries (first-line only, no time). Each row has an id; duplicates collapse via stack=true. Filter severity with levels (default: Error, Exception, Assert, Warning). Exact contains values error/exception/warning/issue are treated as severity filters, not message substring search.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "maxEntries": {
        "type": "integer"
      },
      "levels": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "Error",
            "Assert",
            "Warning",
            "Log",
            "Exception",
            "ConsoleErrors",
            "ConsoleIssues"
          ]
        }
      },
      "contains": {
        "type": "string"
      },
      "stack": {
        "type": "boolean",
        "default": true
      },
      "lastMinutes": {
        "type": "integer"
      }
    }
  }
}
