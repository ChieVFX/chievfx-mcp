{
  "name": "package-remove",
  "description": "Removes an eligible direct Unity Package Manager dependency by package id.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "packageId": {
        "type": "string"
      }
    },
    "required": [
      "packageId"
    ]
  }
}
