{
  "name": "scene-open",
  "description": "Opens a Unity scene asset by scenePath ('path' as reported by scene-list-available is accepted as an alias), preserving dirty scenes unless explicitly saved first.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "scenePath": {
        "type": "string"
      },
      "mode": {
        "type": "string",
        "enum": [
          "Single",
          "Additive"
        ]
      },
      "saveDirtyScenes": {
        "type": "boolean"
      }
    },
    "required": [
      "scenePath"
    ]
  }
}
