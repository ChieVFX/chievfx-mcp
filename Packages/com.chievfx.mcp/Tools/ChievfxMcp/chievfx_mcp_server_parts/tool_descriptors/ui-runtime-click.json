{
  "name": "ui-runtime-click",
  "description": "Click runtime UI by explicit path/id/visualElementRef or by x,y. framework all/auto tries both stacks but returns only framework that resolves explicit path. Requires Play Mode. clicked:true if handler responded (handlerObject=which); clicked:false+resolved:true = hit but nothing handled it.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "framework": {
        "type": "string",
        "enum": ["all", "auto", "ugui", "uitoolkit"],
        "default": "all",
        "description": "Framework scope. all/auto (default) tries uGUI and UI Toolkit; ugui/uitoolkit force one."
      },
      "x": {
        "type": "number",
        "description": "Bottom-left-origin screen x in pixels, or 0..1 when isNormalized is true."
      },
      "y": {
        "type": "number",
        "description": "Bottom-left-origin screen y in pixels, or 0..1 when isNormalized is true."
      },
      "isNormalized": {
        "type": "boolean",
        "default": false,
        "description": "When true, x/y are normalized bottom-left coordinates where 1 is screen width/height."
      },
      "path": {
        "type": "string",
        "description": "uGUI GameObject transform path, UI Toolkit VisualElement path, or visualElementRef (ve:...) from runtime reads/probes."
      },
      "instanceId": {
        "type": "integer",
        "description": "Optional uGUI GameObject instance id."
      },
      "handler": {
        "type": "string",
        "enum": ["pointerClick", "submit"],
        "default": "pointerClick",
        "description": "Click dispatch. pointerClick sends pointer enter/down/up/click (uGUI) or pointer events (UI Toolkit). submit sends EventSystem submit (uGUI) or NavigationSubmitEvent (UI Toolkit)."
      }
    },
    "additionalProperties": true
  }
}
