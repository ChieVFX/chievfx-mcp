{
  "name": "prefab-open",
  "description": "Opens a prefab asset in Unity Prefab Mode, refusing to discard dirty prefab stages.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "prefabPath": {
        "type": "string"
      }
    },
    "required": [
      "prefabPath"
    ]
  }
}
