{
  "name": "script-execute",
  "description": "Last resort: check chievfx://instructions/core-descriptors first \u2014 a purpose-built tool usually exists. Compiles and runs caller-provided C# without writing project files. Entry point convention: a static Main method (any access level, any return type); otherwise pass methodName/className. A script defining exactly one public static method runs it without needing Main.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "csharpCode": {
        "type": "string"
      },
      "className": {
        "type": "string"
      },
      "methodName": {
        "type": "string"
      },
      "parameters": {
        "type": "array",
        "items": {
          "$chievfxSchema": "serializedValue"
        }
      },
      "includeLogs": {
        "type": "boolean"
      },
      "logType": {
        "type": "string",
        "enum": [
          "Error",
          "Assert",
          "Warning",
          "Log",
          "Exception"
        ]
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 100,
        "maximum": {
          "$chievfxConst": "HARD_SCRIPT_EXECUTE_TIMEOUT_MS"
        },
        "default": {
          "$chievfxConst": "DEFAULT_SCRIPT_EXECUTE_TIMEOUT_MS"
        }
      }
    },
    "required": [
      "csharpCode"
    ]
  }
}
