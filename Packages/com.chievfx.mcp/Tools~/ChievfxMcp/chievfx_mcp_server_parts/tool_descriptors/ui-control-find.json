{
  "name": "ui-control-find",
  "description": "Find enabled, on-screen uGUI and UI Toolkit controls with compact paths, control types, and bottom-left screen click zones. Returns 10 controls per page; pass page to fetch more.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "outputFormat": {
        "$chievfxSchema": "outputFormat"
      },
      "framework": {
        "type": "string",
        "enum": ["all", "ugui", "uitoolkit"],
        "default": "all",
        "description": "Framework scope. all (default) searches uGUI and UI Toolkit."
      },
      "wildcards": {
        "description": "Optional wildcard filter on control name or compact path. Use * and ? like gameobject-find namePattern; a pattern without * or ? must match the full name or path exactly (use *Lane1* for a contains search). String or string array; matches if any pattern hits. When nothing matches, the result says so explicitly (matched:false) instead of listing other controls.",
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          }
        ]
      },
      "controlType": {
        "type": "string",
        "description": "Optional control type filter, e.g. button, toggle, slider, dropdown, inputfield, scrollrect."
      },
      "page": {
        "type": "integer",
        "minimum": 1,
        "default": 1,
        "description": "1-based page index. Each page returns up to 10 enabled on-screen controls across all frameworks."
      },
      "normalizeCoords": {
        "type": "boolean",
        "default": false,
        "description": "When true, zone bounds are normalized 0..1 from bottom-left (0) to top-right (1) using the current screen size. Text output uses D.DD except exact 0 and 1."
      }
    },
    "additionalProperties": false
  }
}
