# ChievFX MCP — Python 3.9+ required.
# The MCP server (Tools~/ChievfxMcp/chievfx_mcp_server.py) uses the standard library only;
# no third-party packages are required for Cursor/Claude/Codex MCP stdio or HTTP transport.
#
# Optional PyQt drag-and-drop installer GUI:
#   pip install -r Install~/requirements.txt
